# Prevent native module compilation on Vercel
target_platform=linux
target_arch=x64
cache_lock_stale=60000
network_timeout=300000

# Skip optional dependencies that might cause build issues
optional=false

# Disable package-lock for faster installs
package-lock=false